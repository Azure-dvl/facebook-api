APP_NAME=Facebook API
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG=false

DB_HOST=localhost
DB_PORT=5432
DB_USER=fb_user
DB_PASSWORD=changeme
DB_NAME=facebook_api

ENCRYPTION_KEY=generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"


# Solo llenar si la API esta hosteada en un servidor remoto: la URL con la que
# se accede desde afuera (la que va en el QR y los links).
# Ej: APP_PUBLIC_URL=https://api.midominio.com
# Si esta vacia se usa http://localhost:PORT
APP_PUBLIC_URL=

# ============================================================================
# HOSTING (Render.com) - SOLO TESTING
# Estos valores no van aqui: se llenan manualmente en el panel de Render
# (env vars del servicio). Lista completa en HOSTING.md.
# TODO ejemplos de TESTING (ponlos en Render):
#   DB_HOST=        <- host interno del Postgres de facebook-api
#   DB_PORT=5432
#   DB_USER=        <- usuario del Postgres
#   DB_PASSWORD=    <- password del Postgres
#   DB_NAME=facebook_api
#   ENCRYPTION_KEY= <- python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
#   APP_PUBLIC_URL=https://tu-facebook-api.onrender.com
# ============================================================================